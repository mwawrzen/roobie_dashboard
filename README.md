
# Roobie CMS - Dashboard

A modern, responsive administrative panel for Roobie CMS, built with [Next.js](https://nextjs.org/)


## 🛠️ Tech Stack

* **Next.js**: React framework with App Router and Server Components.
* **Shadcn**: Beautifully-designed, accessible and customizable component library.
* **Tailwind CSS**: Utility-first CSS framework for styling.
* **Bun**: Used for building and dependency management.
## 🚀 Run Locally

1. Install dependencies: `bun install`
2. Start dev server: `bun run dev`


## 📦 Build Process

The project is configured with `output: 'standalone'` in `next.config.js`. This creates a minimal production build that includes only the necessary `node_modules` for execution.

See more in distribution repository: [roobie_dist](https://github.com/mwawrzen/roobie_dist)
